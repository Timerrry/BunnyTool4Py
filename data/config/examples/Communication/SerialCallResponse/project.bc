{"author":"timmery","board":"ArduinoUNO","createdAt":1531280959,"files":[{"content":"int firstSensor = 0;    //第一个模拟传感器\nint secondSensor = 0;   //第二个模拟传感器\nint thirdSensor = 0;    //第三个模拟传感器\nint inByte = 0;         //传入的串口的字节\n\nvoid setup() {\n  //初始化串口，波特率为9600\n  Serial.begin(9600);\n  while (!Serial) {\n    ; //等待串口连接\n  }\n\n  pinMode(2, INPUT);   //数字传感器位于数字引脚2上\n  establishContact();  //发送一个字节以建立连接，直到接收器响应\n}\n\nvoid loop() {\n  //如果我们得到一个有效的数据，则进行下一步\n  if (Serial.available() > 0) {\n    //得到传入的数据\n    inByte = Serial.read();\n    //读取第一个模拟量输入，除以4使范围为0-255\n    firstSensor = analogRead(A0) / 4;\n    //延迟10ms\n    delay(10);\n    //读取第二个模拟输入，除以4使范围为0-255\n    secondSensor = analogRead(1) / 4;\n    //读取开关，将其映射到0到255\n    thirdSensor = map(digitalRead(2), 0, 1, 0, 255);\n    //发送传感器值\n    Serial.write(firstSensor);\n    Serial.write(secondSensor);\n    Serial.write(thirdSensor);\n  }\n}\n\nvoid establishContact() {\n  while (Serial.available() <= 0) {\n    Serial.print('A');   //发送大写字母A\n    delay(300);\n  }\n}\n","language":"cpp","main":true,"name":"SerialCallResponse.ino"}],"index":0,"name":"SerialCallResponse","updatedAt":1531281020}
